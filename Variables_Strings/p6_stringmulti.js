// Using + operator
const message = `Hello` + `Devasya` ;
console.log(message);

// Simple multi-line string
let str = `hello
     How are you
                I am fine`;
                
console.log(str);

// Use of another template literals 

var a = 10;
console.log("A value is \n" +  a);
// Variables or expressions can be placed inside the string using the ${...}
console.log(`A value is ${a} `);